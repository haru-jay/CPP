# C++ - Module 09
## STL

> 이 문서는 C++ 모듈 09의 연습 과제를 담고 있습니다. (원본: cpp09.pdf, Version 3.1)

---

## 목차

- [I. 소개](#i-소개)
- [II. 공통 규칙](#ii-공통-규칙)
- [III. 모듈별 규칙](#iii-모듈별-규칙)
- [IV. AI 사용 지침](#iv-ai-사용-지침)
- [V. 연습 00: Bitcoin Exchange](#v-연습-00-bitcoin-exchange)
- [VI. 연습 01: Reverse Polish Notation](#vi-연습-01-reverse-polish-notation)
- [VII. 연습 02: PmergeMe](#vii-연습-02-pmergeme)
- [VIII. 제출 및 동료 평가](#viii-제출-및-동료-평가)

---

## I. 소개

> C++는 Bjarne Stroustrup이 C 프로그래밍 언어의 확장, 즉 "클래스가 있는 C(C with Classes)"로 만든 범용 프로그래밍 언어입니다. (출처: Wikipedia)

이 모듈들의 목표는 여러분에게 **객체지향 프로그래밍(Object-Oriented Programming)** 을 소개하는 것입니다. 이것이 여러분의 C++ 여정의 출발점이 될 것입니다. OOP를 배우기 위해 추천되는 언어는 많이 있습니다. 우리는 C++가 여러분의 오랜 친구인 C에서 파생된 언어이기 때문에 C++를 선택했습니다. C++는 복잡한 언어이므로, 내용을 단순하게 유지하기 위해 여러분의 코드는 C++98 표준을 따라야 합니다.

현대의 C++가 여러 면에서 많이 다르다는 것을 우리도 알고 있습니다. 그러므로 숙련된 C++ 개발자가 되고 싶다면, 42 Common Core 이후에 더 나아가는 것은 여러분의 몫입니다!

---

## II. 공통 규칙

### 컴파일

- `c++` 컴파일러와 `-Wall -Wextra -Werror` 플래그로 코드를 컴파일할 것
- `-std=c++98` 플래그를 추가해도 코드는 여전히 컴파일되어야 함

### 형식 및 명명 규칙

- 연습 디렉터리는 `ex00`, `ex01`, ... , `exn` 형식으로 명명됩니다.
- 가이드라인에서 요구하는 대로 파일, 클래스, 함수, 멤버 함수, 속성의 이름을 지정하세요.
- 클래스 이름은 **UpperCamelCase** 형식으로 작성합니다. 클래스 코드를 담은 파일은 항상 클래스 이름에 따라 명명됩니다. 예: `ClassName.hpp`/`ClassName.h`, `ClassName.cpp`, 또는 `ClassName.tpp`. 벽돌 벽을 나타내는 "BrickWall" 클래스를 정의하는 헤더 파일이 있다면, 그 이름은 `BrickWall.hpp`가 됩니다.
- 별도로 명시되지 않는 한, 모든 출력 메시지는 개행 문자로 끝나야 하며 표준 출력에 표시되어야 합니다.
- *굿바이 Norminette!* C++ 모듈에서는 어떤 코딩 스타일도 강제되지 않습니다. 여러분이 가장 좋아하는 스타일을 따라도 됩니다. 하지만 여러분의 동료 평가자가 이해할 수 없는 코드는 그들이 채점할 수 없는 코드라는 점을 명심하세요. 깔끔하고 읽기 쉬운 코드를 작성하도록 최선을 다하세요.

### 허용/금지 사항

여러분은 더 이상 C로 코딩하지 않습니다. 이제 C++로 넘어갈 시간입니다! 따라서:

- 표준 라이브러리의 거의 모든 것을 사용할 수 있습니다. 따라서 이미 알고 있는 것에만 매달리지 말고, 여러분이 익숙하게 써온 C 함수들의 C++ 스타일 버전을 최대한 사용하는 것이 현명할 것입니다.
- 다만, 다른 어떤 외부 라이브러리도 사용할 수 없습니다. 즉 C++11(및 그 파생 형태)과 `Boost` 라이브러리는 금지됩니다. 다음 함수들도 금지됩니다: `*printf()`, `*alloc()`, `free()`. 이를 사용하면 점수는 0점이며 그걸로 끝입니다.
- 별도로 명시되지 않는 한 `using namespace <ns_name>`과 `friend` 키워드는 금지된다는 점에 유의하세요. 위반 시 점수는 -42점입니다.
- **STL은 Module 08과 09에서만 사용이 허용됩니다.** 즉, 그 전까지는 **컨테이너**(vector/list/map 등)와 **알고리즘**(`<algorithm>` 헤더를 포함해야 하는 모든 것)을 사용할 수 없습니다. 위반 시 점수는 -42점입니다.

### 몇 가지 설계 요구사항

- C++에서도 메모리 누수는 발생합니다. `new` 키워드로 메모리를 할당할 때는 **메모리 누수**를 피해야 합니다.
- Module 02부터 Module 09까지, 별도로 명시되지 않는 한 여러분의 클래스는 **Orthodox Canonical Form(정통 정규 형식)** 으로 설계되어야 합니다.
- 헤더 파일에 넣은 함수 구현(함수 템플릿 제외)은 해당 연습에 대해 0점을 의미합니다.
- 여러분의 각 헤더는 다른 헤더와 독립적으로 사용할 수 있어야 합니다. 따라서 필요한 모든 의존성을 포함해야 합니다. 다만 **include guard**를 추가하여 이중 include 문제는 피해야 합니다. 이를 어기면 점수는 0점입니다.

### Read me

- 필요하다면(예: 코드를 분리하기 위해) 추가 파일을 더할 수 있습니다. 이 과제들은 프로그램으로 검증되지 않으므로, 필수 파일들만 제출한다면 자유롭게 그렇게 해도 됩니다.
- 때때로 어떤 연습의 가이드라인은 짧아 보이지만, 예시들이 지침에 명시적으로 쓰여있지 않은 요구사항을 보여줄 수 있습니다.
- 시작하기 전에 각 모듈을 완전히 읽으세요! 정말로, 꼭 그렇게 하세요.
- 오딘이시여, 토르시여! 머리를 쓰세요!!!

> ⚠️ **주의**: C++ 프로젝트의 Makefile에 관해서는 C에서와 동일한 규칙이 적용됩니다 (Norm 챕터의 Makefile 부분 참고).

> 💡 **팁**: 여러분은 많은 클래스를 구현해야 할 것입니다. 좋아하는 텍스트 에디터를 스크립트화할 수 없다면 이는 지루한 작업처럼 보일 수 있습니다.

> ℹ️ **정보**: 연습 문제를 완료하는 데 있어 어느 정도 자유가 주어집니다. 하지만 필수 규칙은 따르고 게으름 피우지 마세요. 그렇지 않으면 많은 유용한 정보를 놓치게 될 것입니다! 이론적 개념에 대해 읽는 것을 주저하지 마세요.

---

## III. 모듈별 규칙

이 모듈의 각 연습을 수행하려면 표준 컨테이너를 사용하는 것이 **필수**입니다.

한 번 사용된 컨테이너는 이 모듈의 나머지 부분에서는 다시 사용할 수 없습니다.

> ℹ️ **정보**: 연습을 시작하기 전에 주제 전체를 읽어보는 것이 좋습니다.

> ⚠️ **주의**: 연습 02를 제외한 각 연습에서 최소 하나의 컨테이너를 사용해야 하며, 연습 02는 두 개의 컨테이너 사용을 요구합니다.

각 프로그램에 대해 `-Wall`, `-Wextra`, `-Werror` 플래그로 소스 파일들을 요구되는 출력물로 컴파일하는 `Makefile`을 제출해야 합니다.

`c++`를 사용해야 하며, 여러분의 `Makefile`은 다시 링크(relink)하지 않아야 합니다.

`Makefile`은 최소한 `$(NAME)`, `all`, `clean`, `fclean`, `re` 규칙을 포함해야 합니다.

---

## IV. AI 사용 지침

### 배경 (Context)

이 프로젝트는 여러분이 42 훈련 과정의 기초적인 구성 요소들을 발견하는 것을 돕기 위해 설계되었습니다.

핵심 지식과 기술을 제대로 다지기 위해서는, AI 도구와 지원을 사용하는 데 있어 신중한 접근 방식을 취하는 것이 필수적입니다.

진정한 기초 학습에는 도전, 반복, 동료 학습 교류를 통한 진정한 지적 노력이 필요합니다.

AI에 대한 우리의 입장 — 학습 도구로서, 42 훈련의 일부로서, 그리고 취업 시장에서의 기대치로서 — 에 대한 더 완전한 개요는 인트라넷의 전용 FAQ를 참고하세요.

### 핵심 메시지

- 지름길 없이 탄탄한 기초를 쌓으세요.
- 기술 및 역량을 진짜로 발전시키세요.
- 진정한 동료 학습을 경험하고, 배우는 법과 새로운 문제를 해결하는 법을 배우기 시작하세요.
- 결과보다 학습 여정이 더 중요합니다.
- AI와 관련된 위험에 대해 배우고, 흔한 함정을 피하기 위한 효과적인 통제 방법과 대응책을 개발하세요.

### 학습자 규칙

- AI에 의지하기 전에, 특히 그 전에 여러분에게 주어진 과제에 스스로 사고력을 적용해야 합니다.
- AI에게 직접적인 답을 요구해서는 안 됩니다.
- 42의 AI에 대한 전반적인 접근 방식에 대해 배워야 합니다.

### 이 단계에서 얻는 결과 (Phase outcomes)

이 기초 단계 내에서, 여러분은 다음과 같은 결과를 얻게 됩니다:

- 적절한 기술 및 코딩 기초를 갖추게 됩니다.
- 이 단계에서 AI가 왜, 어떻게 위험할 수 있는지 알게 됩니다.

### 코멘트 및 예시

- 네, 우리도 AI가 존재한다는 것을 압니다 — 그리고 그것이 여러분의 프로젝트를 풀어줄 수 있다는 것도 압니다. 하지만 여러분은 여기 배우러 온 것이지, AI가 학습했다는 것을 증명하러 온 것이 아닙니다. 주어진 문제를 AI가 풀 수 있다는 것을 보여주기 위해 여러분의 시간(혹은 우리의 시간)을 낭비하지 마세요.
- 42에서의 학습은 답을 아는 것에 관한 것이 아니라 — 답을 찾는 능력을 개발하는 것에 관한 것입니다. AI는 답을 곧바로 주지만, 그것은 여러분 자신의 사고력을 기르는 것을 막습니다. 그리고 사고에는 시간, 노력이 들며 실패도 수반됩니다. 성공으로 가는 길은 쉽게 되어 있지 않습니다.
- 시험 중에는 AI를 사용할 수 없다는 것을 명심하세요 — 인터넷도, 스마트폰도 없습니다. 학습 과정에서 AI에 너무 많이 의존했다면 여러분은 금방 그것을 깨닫게 될 것입니다.
- 동료 학습은 여러분을 다양한 아이디어와 접근 방식에 노출시켜, 대인 관계 능력과 발산적으로 사고하는 능력을 향상시킵니다. 이는 그저 봇과 채팅하는 것보다 훨씬 더 가치 있습니다. 그러니 부끄러워하지 말고 — 이야기하고, 질문하고, 함께 배우세요!
- 네, AI는 커리큘럼의 일부가 될 것입니다 — 학습 도구로서도, 그 자체로 하나의 주제로서도요. 여러분은 자신만의 AI 소프트웨어를 만들 기회도 갖게 될 것입니다. 우리의 점진적(crescendo) 접근 방식에 대해 더 알고 싶다면 인트라넷에서 제공되는 문서를 살펴보세요.

**✓ 좋은 사례:**
> 새로운 개념에서 막혔다. 근처에 있는 사람에게 어떻게 접근했는지 물어본다. 우리는 10분 동안 이야기를 나눈다 — 그리고 갑자기 이해가 된다. 알게 됐다.

**✗ 나쁜 사례:**
> 몰래 AI를 사용해서, 맞아 보이는 코드를 복사한다. 동료 평가 중에는 아무것도 설명하지 못한다. 낙제한다. 시험에서는 — AI가 없다 — 다시 막힌다. 낙제한다.

---

## V. 연습 00: Bitcoin Exchange

| | |
|---|---|
| **연습** | 00 |
| **제목** | Bitcoin Exchange |
| **디렉터리** | `ex00/` |
| **제출 파일** | `Makefile`, `main.cpp`, `BitcoinExchange.{cpp, hpp}` |
| **금지 사항** | 없음 |

특정 날짜의 특정 양의 비트코인 가치를 출력하는 프로그램을 만들어야 합니다.

이 프로그램은 시간에 따른 비트코인 가격을 나타내는 csv 형식의 데이터베이스를 사용해야 합니다. 이 데이터베이스는 이 주제와 함께 제공됩니다.

프로그램은 평가할 서로 다른 가격/날짜들을 저장한 두 번째 데이터베이스를 입력으로 받습니다.

프로그램은 다음 규칙을 준수해야 합니다:

- 프로그램 이름은 `btc`입니다.
- 프로그램은 파일을 인자로 받아야 합니다.
- 이 파일의 각 줄은 `"date | value"` 형식을 사용해야 합니다.
- 유효한 날짜는 항상 `Year-Month-Day` 형식입니다.
- 유효한 값은 float 또는 0에서 1000 사이의 양의 정수여야 합니다.

> ⚠️ **주의**: 이 연습을 검증하는 코드에는 최소 하나의 컨테이너를 사용해야 합니다. 발생 가능한 오류들은 적절한 오류 메시지로 처리해야 합니다.

### `input.txt` 파일 예시:

```
$> head input.txt
date | value
2011-01-03 | 3
2011-01-03 | 2
2011-01-03 | 1
2011-01-03 | 1.2
2011-01-09 | 1
2012-01-11 | -1
2001-42-42
2012-01-11 | 1
2012-01-11 | 2147483648
$>
```

프로그램은 입력 파일의 값을 사용합니다.

프로그램은 여러분의 데이터베이스에 표시된 날짜에 따른 환율을 값에 곱한 결과를 표준 출력에 표시해야 합니다.

> 💡 **팁**: 입력에 사용된 날짜가 여러분의 DB에 존재하지 않는다면, DB에 포함된 것 중 가장 가까운 날짜를 사용해야 합니다. 상한 날짜가 아니라 하한 날짜를 사용하도록 주의하세요.

### 프로그램 사용 예시:

```
$> ./btc
Error: could not open file.
$> ./btc input.txt
2011-01-03 => 3 = 0.9
2011-01-03 => 2 = 0.6
2011-01-03 => 1 = 0.3
2011-01-03 => 1.2 = 0.36
2011-01-09 => 1 = 0.32
Error: not a positive number.
Error: bad input => 2001-42-42
2012-01-11 => 1 = 7.1
Error: too large a number.
$>
```

> ⚠️ **경고**: 이 연습을 검증하는 데 사용한 컨테이너(들)는 이 모듈의 나머지 부분에서 더 이상 사용할 수 없습니다.

---

## VI. 연습 01: Reverse Polish Notation

| | |
|---|---|
| **연습** | 01 |
| **제목** | RPN |
| **디렉터리** | `ex01/` |
| **제출 파일** | `Makefile`, `main.cpp`, `RPN.{cpp, hpp}` |
| **금지 사항** | 없음 |

다음 제약 조건을 갖는 프로그램을 만들어야 합니다:

- 프로그램 이름은 `RPN`입니다.
- 프로그램은 역폴란드 표기법(inverted Polish) 수식을 인자로 받아야 합니다.
- 이 연산에 사용되고 인자로 전달되는 숫자들은 항상 10 미만입니다. 계산 자체나 결과는 이 규칙을 고려하지 않습니다(즉, 10 이상이 될 수 있습니다).
- 프로그램은 이 수식을 처리하여 올바른 결과를 표준 출력에 출력해야 합니다.
- 프로그램 실행 중 오류가 발생하면 오류 메시지가 표준 에러(standard error)에 표시되어야 합니다.
- 프로그램은 다음 토큰을 사용한 연산을 처리할 수 있어야 합니다: `"+ - / *"`.

> ⚠️ **주의**: 이 연습을 검증하는 코드에는 최소 하나의 컨테이너를 사용해야 합니다.

> ℹ️ **정보**: 괄호나 소수점 숫자를 처리할 필요는 없습니다.

### 일반적인 사용 예시:

```
$> ./RPN "8 9 * 9 - 9 - 4 - 1 +"
42
$> ./RPN "7 7 * 7 -"
42
$> ./RPN "1 2 * 2 / 2 * 2 4 - +"
0
$> ./RPN "(1 + 1)"
Error
$>
```

> ⚠️ **경고**: 이전 연습에서 사용한 컨테이너(들)는 여기서 금지됩니다. 이 연습을 검증하는 데 사용한 컨테이너(들)는 이 모듈의 나머지 부분에서 사용할 수 없게 됩니다.

---

## VII. 연습 02: PmergeMe

| | |
|---|---|
| **연습** | 02 |
| **제목** | PmergeMe |
| **디렉터리** | `ex02/` |
| **제출 파일** | `Makefile`, `main.cpp`, `PmergeMe.{cpp, hpp}` |
| **금지 사항** | 없음 |

다음 제약 조건을 갖는 프로그램을 만들어야 합니다:

- 프로그램 이름은 `PmergeMe`입니다.
- 프로그램은 양의 정수 수열을 인자로 사용할 수 있어야 합니다.
- 프로그램은 이 양의 정수 수열을 정렬하기 위해 **merge-insert sort** 알고리즘을 사용해야 합니다.

> ⚠️ **주의**: 명확히 말하면, 여러분은 **Ford-Johnson 알고리즘**을 사용해야 합니다. (출처: *The Art Of Computer Programming, Vol.3.* Merge Insertion, Page 184.)

- 프로그램 실행 중 오류가 발생하면 오류 메시지가 표준 에러에 표시되어야 합니다.

> ⚠️ **주의**: 이 연습을 검증하는 코드에는 최소 두 개의 서로 다른 컨테이너를 사용해야 합니다. 여러분의 프로그램은 최소 3000개의 서로 다른 정수를 처리할 수 있어야 합니다.

> 💡 **팁**: 각 컨테이너에 대해 알고리즘을 별도로 구현하여, 범용(generic) 함수를 사용하는 것을 피하는 것이 강력히 권장됩니다.

표준 출력에 한 줄씩 표시해야 하는 정보에 대한 추가 지침은 다음과 같습니다:

- 첫 번째 줄에는 명시적인 텍스트와 함께 정렬되지 않은 양의 정수 수열을 표시해야 합니다.
- 두 번째 줄에는 명시적인 텍스트와 함께 정렬된 양의 정수 수열을 표시해야 합니다.
- 세 번째 줄에는, 양의 정수 수열을 정렬하는 데 사용한 첫 번째 컨테이너를 명시하며 알고리즘이 소요한 시간을 나타내는 명시적인 메시지를 표시해야 합니다.
- 마지막 줄에는 두 번째로 사용한 컨테이너를 명시하며 알고리즘이 사용한 시간을 나타내는 명시적인 텍스트를 표시해야 합니다.

> ℹ️ **정보**: 정렬에 소요된 시간을 표시하는 형식은 자유이지만, 선택한 정밀도는 사용된 두 컨테이너 간의 차이를 명확히 볼 수 있어야 합니다.

### 표준 사용 **예시**:

```
$> ./PmergeMe 3 5 9 7 4
Before:  3 5 9 7 4
After:   3 4 5 7 9
Time to process a range of  5 elements with std::[..] :  0.00031 us
Time to process a range of  5 elements with std::[..] :  0.00014 us
$> ./PmergeMe `shuf -i 1-100000 -n 3000 | tr "\n" " "`
Before:  141 79 526 321 [...]
After:   79 141 321 526 [...]
Time to process a range of 3000 elements with std::[..] : 62.14389 us
Time to process a range of 3000 elements with std::[..] : 69.27212 us
$> ./PmergeMe "-1" "2"
Error
$> # For OSX USER:
$> ./PmergeMe `jot -r 3000 1 100000 | tr '\n' ' '`
[...]
$>
```

> 💡 **팁**: 이 예시에서 시간 표시는 의도적으로 이상하게 되어 있습니다. 물론 여러분은 정렬 부분과 데이터 관리 부분을 포함한, 모든 연산에 소요된 시간을 나타내야 합니다.

> ⚠️ **경고**: 이전 연습들에서 사용한 컨테이너(들)는 여기서 금지됩니다.

> ℹ️ **정보**: 중복(duplicate)과 관련된 오류 처리는 여러분의 재량에 맡깁니다.

---

## VIII. 제출 및 동료 평가

평소와 같이 여러분의 `Git` 저장소에 과제를 제출하세요. 평가(defense) 중에는 저장소 안의 작업물만 평가됩니다. 폴더와 파일의 이름이 올바른지 다시 한번 확인하는 것을 주저하지 마세요.

평가 중, 프로젝트에 대한 간단한 **수정**이 가끔 요청될 수 있습니다. 이는 사소한 동작 변경, 몇 줄의 코드를 작성하거나 다시 작성하는 것, 또는 추가하기 쉬운 기능을 포함할 수 있습니다.

이 단계가 **모든 프로젝트에 적용되지는 않을 수 있지만**, 평가 가이드라인에 언급되어 있다면 이에 대비해야 합니다.

이 단계는 프로젝트의 특정 부분에 대한 여러분의 실제 이해도를 확인하기 위한 것입니다. 수정은 여러분이 선택한 어떤 개발 환경에서든(예: 여러분이 평소 사용하는 환경) 수행될 수 있으며, 평가의 일부로 특정 시간 제한이 정의되지 않는 한 몇 분 안에 수행 가능해야 합니다. 예를 들어 함수나 스크립트를 소규모로 업데이트하거나, 화면 표시를 수정하거나, 새로운 정보를 저장하도록 데이터 구조를 조정하는 등의 요청을 받을 수 있습니다.

세부 사항(범위, 대상 등)은 **평가 가이드라인**에 명시되며 동일한 프로젝트라도 평가마다 다를 수 있습니다.
