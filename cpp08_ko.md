# C++ - Module 08 한국어 번역

## 템플릿 컨테이너, 이터레이터, 알고리즘

### 요약

이 문서는 C++ 모듈 08의 과제들을 담고 있다.

버전: 10.1

## 목차

1. 서론
2. 일반 규칙
3. 모듈별 규칙
4. AI 사용 지침
5. Exercise 00: Easy find
6. Exercise 01: Span
7. Exercise 02: Mutated abomination
8. 제출 및 동료 평가

---

## 1장. 서론

C++는 Bjarne Stroustrup이 만든 범용 프로그래밍 언어다. C 프로그래밍 언어의 확장판, 즉 “클래스를 사용하는 C”로 시작했다(출처: Wikipedia).

이 모듈들의 목표는 객체 지향 프로그래밍을 소개하는 것이다. 이것이 여러분의 C++ 여정의 출발점이 된다. 객체 지향 프로그래밍을 배우기 위한 언어는 많지만, C++는 여러분의 오랜 친구인 C에서 파생되었기 때문에 선택했다.

C++는 복잡한 언어이므로, 단순하게 시작하기 위해 이 모듈의 코드는 C++98 표준을 따른다. 현대 C++가 여러 면에서 매우 달라졌다는 점은 알고 있다. 능숙한 C++ 개발자가 되고 싶다면 42 Common Core 이후에도 스스로 더 공부해야 한다.

## 2장. 일반 규칙

### 컴파일

- `c++`와 `-Wall -Wextra -Werror` 플래그로 컴파일해야 한다.
- `-std=c++98` 플래그를 추가해도 코드가 컴파일되어야 한다.

### 형식 및 이름 규칙

- 과제 디렉터리 이름은 `ex00`, `ex01`, ..., `exn` 형식이어야 한다.
- 지침에 요구된 파일, 클래스, 함수, 멤버 함수, 속성 이름을 사용해야 한다.
- 클래스 이름은 UpperCamelCase 형식을 사용한다. 클래스 코드를 담는 파일은 클래스 이름에 맞춰 이름을 짓는다. 예를 들어 `ClassName.hpp`/`ClassName.h`, `ClassName.cpp`, `ClassName.tpp`와 같은 형식이다. `BrickWall`이라는 클래스를 정의하는 헤더라면 `BrickWall.hpp`로 이름 짓는다.
- 별도로 지정하지 않는 한 모든 출력 메시지는 개행 문자로 끝나야 하며 표준 출력으로 표시해야 한다.
- C++ 모듈에서는 Norminette를 사용하지 않는다. 원하는 코딩 스타일을 따라도 된다. 다만 동료 평가자가 이해하지 못하는 코드는 평가할 수 없다. 깨끗하고 읽기 쉬운 코드를 작성하라.

### 허용 및 금지 사항

이제 C가 아니라 C++로 코딩한다.

- 표준 라이브러리는 거의 모두 사용할 수 있다. 익숙한 C 함수에만 매달리기보다 가능한 한 C++ 방식의 대응 기능을 사용하는 것이 좋다.
- 다른 외부 라이브러리는 사용할 수 없다. C++11 및 그 이후 표준과 Boost 라이브러리는 금지된다. `*printf()`, `*alloc()`, `free()` 함수도 금지된다. 사용하면 점수는 0점이다.
- 별도의 명시가 없다면 `using namespace <ns_name>`과 `friend` 키워드는 금지된다. 위반 시 점수는 -42점이다.
- STL은 모듈 08과 09에서만 사용할 수 있다. 그 전까지는 `vector`, `list`, `map` 등의 컨테이너와 `<algorithm>` 헤더를 필요로 하는 알고리즘을 사용할 수 없다. 위반 시 점수는 -42점이다.

### 설계 요구사항

- C++에서도 메모리 누수는 발생한다. `new`로 메모리를 할당했다면 메모리 누수가 없도록 해야 한다.
- 별도로 명시하지 않는 한 모듈 02부터 09까지의 클래스는 Orthodox Canonical Form으로 설계해야 한다.
- 함수 템플릿을 제외하고 헤더 파일에 함수 구현을 넣으면 해당 과제는 0점이다.
- 각 헤더는 다른 헤더에 의존하지 않고 독립적으로 사용할 수 있어야 한다. 따라서 필요한 모든 의존성을 직접 include해야 한다. 동시에 include guard를 사용해 중복 include 문제를 막아야 한다. 그렇지 않으면 0점이다.

### 읽어 둘 것

- 코드를 나누기 위해 추가 파일을 만들어도 된다. 과제는 프로그램으로 자동 검증되지 않으므로, 필수 파일을 제출하는 한 필요한 파일을 자유롭게 추가할 수 있다.
- 과제 지침은 짧아도 예제에 명시되지 않은 요구사항이 드러나는 경우가 있다.
- 시작하기 전에 모듈 전체를 읽어라. 정말로 읽어라.
- Odin과 Thor의 이름으로! 머리를 써라!

C++ 프로젝트의 Makefile에도 C 프로젝트와 동일한 규칙을 적용한다(Makefile에 관한 Norm 장을 참고하라).

많은 클래스를 구현해야 한다. 좋아하는 텍스트 편집기를 스크립트로 자동화할 수 있다면 이 작업이 덜 지루할 것이다.

과제를 해결하는 방법에는 어느 정도 자유가 있다. 그러나 필수 규칙은 지키고 게으름을 피우지 마라. 유용한 정보를 많이 놓치게 된다. 이론적 개념도 주저하지 말고 읽어라.

## 3장. 모듈별 규칙

이 모듈의 과제는 표준 컨테이너와 표준 알고리즘 없이도 풀 수 있다는 것을 알게 될 것이다.

하지만 이 모듈의 목적은 바로 그것들을 사용하는 것이다. STL, 특히 컨테이너(`vector`, `list`, `map` 등)와 `<algorithm>`에 정의된 알고리즘을 적절한 곳에서 반드시 사용해야 한다. 가능한 한 많이 사용하도록 하라.

작동하더라도 STL을 사용하지 않으면 매우 낮은 점수를 받게 된다. 게으름을 피우지 마라.

템플릿은 평소처럼 헤더 파일에 정의해도 된다. 또는 헤더에는 템플릿 선언만 두고 구현을 `.tpp` 파일에 작성해도 된다. 어느 쪽이든 헤더 파일은 필수이고 `.tpp` 파일은 선택 사항이다.

## 4장. AI 사용 지침

### 배경

이 프로젝트는 42 교육의 기본 요소를 스스로 발견하도록 설계되었다.

핵심 지식과 기술을 제대로 익히려면 AI 도구와 지원을 신중한 방식으로 사용해야 한다.

진정한 기초 학습에는 도전, 반복, 동료 학습을 통한 진짜 지적 노력이 필요하다.

학습 도구로서의 AI, 42 교육의 일부로서의 AI, 직업 시장에서 기대되는 AI 활용에 관한 더 자세한 내용은 인트라넷의 전용 FAQ를 참고하라.

### 핵심 메시지

- 지름길 없이 탄탄한 기초를 쌓아라.
- 진짜 기술력과 코딩 역량을 길러라.
- 42의 동료 학습을 경험하고, 배우는 법과 새로운 문제를 해결하는 법을 익혀라.
- 결과보다 학습 여정이 중요하다.
- AI와 관련된 위험을 배우고, 흔한 실수를 피하기 위한 효과적인 통제 방법과 대응책을 개발하라.

### 학습자 규칙

- 특히 AI를 찾기 전에 먼저 자신에게 주어진 과제에 대해 스스로 추론해야 한다.
- AI에게 직접 답을 요구해서는 안 된다.
- AI에 대한 42의 글로벌 접근법을 배워야 한다.

### 이 단계의 성과

- 올바른 기술 및 코딩 기초를 갖춘다.
- 이 단계에서 AI가 왜, 어떻게 위험할 수 있는지 안다.

### 설명 및 예시

- AI가 존재하고 프로젝트를 해결할 수 있다는 사실은 알고 있다. 하지만 이곳에 온 목적은 AI가 학습했다는 것을 증명하는 것이 아니라 여러분이 배우는 것이다. AI가 문제를 풀 수 있다는 것을 보여 주느라 여러분과 평가자의 시간을 낭비하지 마라.
- 42에서의 학습은 답을 아는 것이 아니라 답을 찾아내는 능력을 기르는 것이다. AI는 답을 바로 주지만, 그 과정에서 스스로 추론하는 능력을 기를 기회를 빼앗는다. 추론에는 시간과 노력이 필요하고 실패도 따른다. 성공으로 가는 길은 원래 쉬워서는 안 된다.
- 시험 중에는 AI를 사용할 수 없다. 인터넷도 스마트폰도 없다. 학습 과정에서 AI에 지나치게 의존했는지는 곧 드러날 것이다.
- 동료 학습은 서로 다른 생각과 접근법을 보여 주므로 대인 관계 능력과 다르게 생각하는 능력을 키운다. 단순히 봇과 대화하는 것보다 훨씬 가치 있다. 주저하지 말고 이야기하고 질문하며 함께 배워라.
- AI는 학습 도구로서뿐 아니라 그 자체가 학습 주제로서도 교육 과정에 포함될 것이다. 직접 AI 소프트웨어를 만들어 볼 기회도 있다. 42의 문서에 설명된 점진적 접근법에 대해 더 알아보라.

좋은 예:

> 새로운 개념에서 막혔다. 가까이 있는 사람에게 어떻게 접근했는지 물어본다. 10분 동안 이야기했더니 갑자기 이해된다.

나쁜 예:

> 몰래 AI를 사용해 그럴듯해 보이는 코드를 복사한다. 동료 평가에서 아무것도 설명하지 못하고 실패한다. 시험에서는 AI를 사용할 수 없으므로 다시 막히고 실패한다.

## 5장. Exercise 00: Easy find

### 과제 00 — Easy find

- 디렉터리: `ex00/`
- 제출 파일: `Makefile`, `main.cpp`, `easyfind.{h, hpp}`
- 선택 파일: `easyfind.tpp`
- 금지 사항: 없음

첫 번째 쉬운 과제로 좋은 출발을 해 보자.

`T` 타입을 받는 함수 템플릿 `easyfind`를 작성하라. 이 함수는 두 매개변수를 받는다. 첫 번째는 `T` 타입이고 두 번째는 정수다.

`T`가 정수 컨테이너라고 가정한다. 이 함수는 첫 번째 매개변수 안에서 두 번째 매개변수와 같은 값이 처음 나타나는 위치를 찾아야 한다.

일치하는 값이 없다면 예외를 던지거나 원하는 오류 값을 반환해도 된다. 도움이 필요하면 표준 컨테이너가 어떻게 동작하는지 살펴보라.

물론 모든 것이 예상대로 동작하는지 확인할 수 있도록 직접 테스트도 작성하여 제출하라.

연관 컨테이너는 처리하지 않아도 된다.

## 6장. Exercise 01: Span

### 과제 01 — Span

- 디렉터리: `ex01/`
- 제출 파일: `Makefile`, `main.cpp`, `Span.{h, hpp}`, `Span.cpp`
- 금지 사항: 없음

최대 `N`개의 정수를 저장할 수 있는 `Span` 클래스를 작성하라. `N`은 `unsigned int` 변수이며 생성자에 전달되는 유일한 매개변수다.

이 클래스에는 숫자 하나를 `Span`에 추가하는 `addNumber()` 멤버 함수가 있어야 한다. 이 함수로 컨테이너를 채운다. 이미 `N`개의 원소가 저장된 상태에서 새 원소를 추가하려고 하면 예외를 던져야 한다.

다음으로 `shortestSpan()`과 `longestSpan()` 두 멤버 함수를 구현하라.

두 함수는 저장된 모든 숫자 사이의 가장 짧은 간격과 가장 긴 간격을 각각 찾아 반환해야 한다. 저장된 숫자가 없거나 하나뿐이면 간격을 구할 수 없으므로 예외를 던져야 한다.

물론 직접 테스트를 작성해야 하며, 아래 예제보다 훨씬 철저해야 한다. 최소 10,000개의 숫자로 `Span`을 테스트하라. 더 많으면 더 좋다.

다음 코드를 실행하면:

```cpp
int main()
{
    Span sp = Span(5);

    sp.addNumber(6);
    sp.addNumber(3);
    sp.addNumber(17);
    sp.addNumber(9);
    sp.addNumber(11);

    std::cout << sp.shortestSpan() << std::endl;
    std::cout << sp.longestSpan() << std::endl;

    return 0;
}
```

다음과 같이 출력되어야 한다.

```text
$> ./ex01
2
14
$>
```

마지막으로, 이터레이터 범위를 사용해 `Span`을 채울 수 있으면 좋을 것이다. 수천 번 `addNumber()`를 호출하는 것은 매우 번거롭다. 한 번의 호출로 여러 숫자를 `Span`에 추가하는 멤버 함수를 구현하라.

힌트가 필요하다면 컨테이너를 공부하라. 일부 멤버 함수는 이터레이터 범위를 받아 원소 시퀀스를 컨테이너에 추가한다.

## 7장. Exercise 02: Mutated abomination

### 과제 02 — Mutated abomination

- 디렉터리: `ex02/`
- 제출 파일: `Makefile`, `main.cpp`, `MutantStack.{h, hpp}`
- 선택 파일: `MutantStack.tpp`
- 금지 사항: 없음

이제 더 진지한 문제로 넘어가 보자. 이상한 것을 만들어 보자.

`std::stack` 컨테이너는 매우 좋다. 하지만 안타깝게도 이터레이션할 수 없는 몇 안 되는 STL 컨테이너 중 하나다. 아쉬운 일이다.

하지만 왜 이를 받아들여야 하는가? 원래의 스택을 마음대로 개조해 부족한 기능을 추가할 수 있는데 말이다.

이 부당함을 바로잡기 위해 `std::stack` 컨테이너를 이터레이션 가능하게 만들어라.

`MutantStack` 클래스를 작성하라. 이 클래스는 `std::stack`을 기반으로 구현되어야 한다. `std::stack`의 모든 멤버 함수를 제공하면서, 추가 기능으로 이터레이터를 제공해야 한다.

물론 모든 것이 예상대로 동작하는지 확인할 수 있도록 직접 테스트도 작성하여 제출하라.

테스트 예시는 다음과 같다.

```cpp
int main()
{
    MutantStack<int> mstack;

    mstack.push(5);
    mstack.push(17);

    std::cout << mstack.top() << std::endl;

    mstack.pop();

    std::cout << mstack.size() << std::endl;

    mstack.push(3);
    mstack.push(5);
    mstack.push(737);
    //[...]
    mstack.push(0);

    MutantStack<int>::iterator it = mstack.begin();
    MutantStack<int>::iterator ite = mstack.end();

    ++it;
    --it;
    while (it != ite)
    {
        std::cout << *it << std::endl;
        ++it;
    }
    std::stack<int> s(mstack);
    return 0;
}
```

이 코드를 처음에는 직접 만든 `MutantStack`으로 실행하고, 다음에는 예를 들어 `std::list`로 바꾸어 실행했을 때 두 출력이 같아야 한다. 물론 다른 컨테이너로 테스트할 때는 그 컨테이너에 맞게 멤버 함수 이름을 바꿔야 한다(`push()`가 `push_back()`이 될 수 있다).

## 8장. 제출 및 동료 평가

평소처럼 Git 저장소에 과제를 제출하라. 평가 중에는 저장소 안의 작업만 사용한다. 폴더와 파일 이름이 올바른지 다시 확인하라.

평가 중 프로젝트를 간단히 수정하라고 요청받을 수 있다. 여기에는 작은 동작 변경, 몇 줄의 코드 작성 또는 수정, 쉽게 추가할 수 있는 기능 등이 포함될 수 있다.

이 단계가 모든 프로젝트에 적용되는 것은 아니지만, 평가 지침에 언급되어 있다면 준비해야 한다.

이 과정의 목적은 프로젝트의 특정 부분을 실제로 이해하고 있는지 확인하는 것이다. 수정은 평소 사용하는 환경 등 원하는 개발 환경에서 수행할 수 있어야 하며, 특별한 시간 제한이 없다면 몇 분 안에 끝낼 수 있는 정도여야 한다.

예를 들어 함수나 스크립트의 작은 업데이트, 출력 화면 변경, 데이터 저장 구조의 간단한 수정 등을 요청받을 수 있다. 구체적인 범위와 대상은 평가 지침에 명시되며, 같은 프로젝트라도 평가마다 달라질 수 있다.
